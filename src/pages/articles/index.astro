---
import type { BreadcrumbComponent } from "@components/Breadcrumb.astro";
import PostListing from "@components/PostListing.astro";
import TaxonomyListingLayout from "@layouts/TaxonomyListingLayout.astro";
import { getContentEntry } from "@lib/entries";
import { $path } from "astro-typesafe-routes/path";

const breadcrumbs: BreadcrumbComponent[] = [
  { label: "index", href: $path({ to: "/site-index" }) },
  [
    { label: "notes", href: $path({ to: "/notes" }) },
    { label: "Articles", styleNormal: true },
  ],
];

const articlesEntry = await getContentEntry("other", "taxonomyArticles");
---

<TaxonomyListingLayout
  pageTitle="Articles"
  displayTitle="All articles"
  breadcrumbs={breadcrumbs}
  rssHref={$path({
    to: "/articles/feed.xml",
    trailingSlash: false,
  })}
  breadcrumbsDescription={articlesEntry?.data.description}
  preambleEntry={articlesEntry}
>
  <PostListing collections={["articles"]} />
</TaxonomyListingLayout>
